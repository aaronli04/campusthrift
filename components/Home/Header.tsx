import React from "react";

import { HStack, Image, Text, VStack } from "@chakra-ui/react";

const Header: React.FC = () => {
  return (
    <HStack w="100%" justifyContent="space-between">
        <Text>
            College stays scamming
        </Text>
    </HStack>
  );
};

export default Header;